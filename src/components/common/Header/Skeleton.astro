---
---

<header class="fixed top-0 left-0 w-full z-40 bg-gray-100 py-3 px-4 sm:px-6 lg:px-8 animate-pulse">
  <div class="container mx-auto flex items-center justify-between h-16">
    <!-- Logo -->
    <div class="flex-shrink-0">
      <div class="h-10 w-32 bg-gray-300 rounded"></div>
    </div>
    
    <!-- Menú Desktop -->
    <div class="hidden md:flex space-x-8">
      {Array.from({ length: 4 }).map(() => (
        <div class="h-6 w-16 bg-gray-300 rounded"></div>
      ))}
    </div>
    
    <!-- Botón Mobile -->
    <div class="md:hidden">
      <div class="h-6 w-6 bg-gray-300 rounded"></div>
    </div>
  </div>
</header>

---
---

<footer class="bg-gray-100 animate-pulse">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
      {Array.from({ length: 4 }).map(() => (
        <div>
          <div class="h-6 w-24 bg-gray-300 rounded mb-4"></div>
          <div class="space-y-2">
            {Array.from({ length: 4 }).map(() => (
              <div class="h-4 w-3/4 bg-gray-300 rounded"></div>
            ))}
          </div>
        </div>
      ))}
    </div>
    
    <div class="mt-12 pt-8 border-t border-gray-200">
      <div class="flex flex-col md:flex-row justify-between items-center">
        <div class="h-6 w-32 bg-gray-300 rounded mb-4 md:mb-0"></div>
        <div class="flex space-x-6">
          {Array.from({ length: 4 }).map(() => (
            <div class="h-6 w-6 bg-gray-300 rounded-full"></div>
          ))}
        </div>
      </div>
    </div>
  </div>
</footer>

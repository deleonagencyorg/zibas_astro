/**
 * Configuración básica para el sistema de internacionalización
 * 
 * Este archivo se mantiene por compatibilidad, pero la implementación
 * real está en el archivo i18n.ts
 */

export const config = {
  defaultLocale: 'es',
  supportedLocales: ['es', 'en'],
  localesPath: '/locales'
};

export default config;
